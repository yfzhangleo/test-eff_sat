packages:
  - package: dbt-labs/logging
    version: 0.5.0
  - git: "https://github.com/Datavault-UK/dbtvault"
    revision: v0.7.6.1
  - git: "{{'https://' ~ env_var('CI_USERNAME', '') ~ ':' ~ env_var('CI_PASSWD', '') ~ '@gitlab.com/' if env_var('CI_USERNAME', '') else
  'git@gitlab.com:'}}mintel/data-warehouse/mintel-dbt-utils.git"
    revision: v2.0.0
